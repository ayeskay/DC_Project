# Replication configuration
# Set replica id (unique integer per replica)
replica.id=1
# Comma-separated list of replicas in form host:port
replicas=localhost:6000,localhost:6001,localhost:6002
# Which replica is leader (by replica.id)
leader.id=1
# Port this replica listens on for incoming GameState updates
listen.port=6000
